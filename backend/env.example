# Copy to .env and set real values. Never commit .env, .env.local, or .env.dev.
# If .env.dev was ever committed: run "git rm --cached backend/.env.dev", then rotate APP_SECRET.
APP_SECRET=change_me_to_a_random_string
DATABASE_URL="postgresql://user:password@127.0.0.1:5432/dbname?serverVersion=16&charset=utf8"
RABBITMQ_DSN=amqp://user:password@localhost:5672/%2F
